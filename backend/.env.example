# Server configuration
PORT=3001
NODE_ENV=development # Uses Firebase emulators in development mode
LOG_LEVEL=info

# Google Cloud configuration
GOOGLE_CLOUD_PROJECT=collegebot-dev-52f43

# Google Maps API configuration
GOOGLE_MAPS_API_KEY=your_maps_api_key_here

# Firebase configuration
# For production, get service account key from Firebase Console:
# 1. Go to Project Settings > Service accounts
# 2. Click "Generate New Private Key"
# Option A: Use credentials file (recommended)
# 3. Save the downloaded JSON file securely and specify its path:
FIREBASE_CREDENTIALS_FILE=/path/to/service-account.json

# Option B: Use base64-encoded credentials in environment variable
# 3. Convert the downloaded JSON to base64:
#    base64 -i path/to/service-account.json | tr -d '\n'
# 4. Add the base64 string here:
# FIREBASE_CREDENTIALS=your_base64_encoded_service_account_json

# AI Service configuration
AI_SERVICE_TYPE=claude # or gemini
CLAUDE_API_KEY=your_claude_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here
CLAUDE_MODEL=claude-3-5-sonnet-20241022
GEMINI_MODEL=gemini-2.0-flash
